/**
 * Factory for transforming rules in {@link JSON} format into validation functions
 *
 * Created by Ondřej Kratochvíl on 10.11.15.
 */
export default class ValidationFunctionFactory {

    /**
     * Transforms given {@link JSON} rule into {@link Rule} validation function
     *
     * @param {JSON} rule
     * @returns {Rule} validation function
     */
    static create(rule) {
        // toDo: implement
    }
}